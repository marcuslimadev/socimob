import React from 'react';

const ConversationDetail = ({ conversationId }) => {
  return (
    <div>
      <h1>Detalhe da Conversa #{conversationId}</h1>
      <p>Exibir dados do lead, imóvel, corretor, timeline, mensagens, notas, tarefas, visitas e propostas.</p>
      {/* Implementar exibição de detalhes da conversa e ações */} 
    </div>
  );
};

export default ConversationDetail;
